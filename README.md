# Vigil
Live Tampering Detection via NTFS Journal Inconsistency is a digital forensics tool that detects anti-forensic activity by analyzing mismatches between NTFS $LogFile and $UsnJrnl records. The system monitors filesystem events in real time and flags suspicious behavior that may indicate hidden file modifications or log tampering.
